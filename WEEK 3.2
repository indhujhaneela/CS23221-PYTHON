unit = (float(input()))
if unit <= 83:
    cost = 100
elif unit > 83 and unit < 200:
    cost = unit * 1.20
elif unit >=200 and unit < 400:
    cost = unit * 1.50
elif unit == 400 :
    cost = unit * 1.80
elif unit > 400 and unit < 600:
    cos = (unit * 1.80 )
    cost = cos + cos*0.15
else:
    cos = unit * 2.00
    cost = cos + cos*0.15
print("%.2f" %float(cost))
